# Server Configuration
PORT=5000

# MongoDB Connection String (leave blank or comment out to run in memory fallback mode)
MONGODB_URI=mongodb://localhost:27017/kidrove-workshop

# CORS Configuration (comma-separated origins allowed to query the API, use '*' for wildcard)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000
